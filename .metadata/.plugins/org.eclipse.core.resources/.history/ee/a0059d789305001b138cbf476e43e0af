package teste_publica;

public class jogos2 {
	
	int plcar,minimo,maximo,recorde_min,recorde_max;

	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + maximo;
		result = prime * result + minimo;
		result = prime * result + plcar;
		result = prime * result + recorde_max;
		result = prime * result + recorde_min;
		return result;
	}

	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (obj == null)
			return false;
		if (getClass() != obj.getClass())
			return false;
		jogos2 other = (jogos2) obj;
		if (maximo != other.maximo)
			return false;
		if (minimo != other.minimo)
			return false;
		if (plcar != other.plcar)
			return false;
		if (recorde_max != other.recorde_max)
			return false;
		if (recorde_min != other.recorde_min)
			return false;
		return true;
	}

}
